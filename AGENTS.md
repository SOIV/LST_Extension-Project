# AGENTS.md

이 문서는 Codex와 다른 코딩 에이전트가 이 저장소에서 작업할 때 따라야 하는 프로젝트 규칙입니다.

## Communication

- 사용자와의 대화, 작업 요약, 커밋 메시지, PR 제목과 설명은 기본적으로 한국어로 작성합니다.
- 코드, 파일명, 명령어, API 이름, 라이브러리 이름, 오류 메시지는 원문을 유지합니다.
- 영어로 작성해야 하는 외부 템플릿이나 자동 생성 문서가 아닌 이상 임의로 영어 문장을 쓰지 않습니다.
- 작업 중 확인한 제약, 실패한 명령, 남은 변경 사항은 짧고 구체적으로 한국어로 보고합니다.

## Commit Messages

- 커밋 메시지는 Conventional Commits 형식을 사용합니다.
- 형식은 `type: 한국어 요약`입니다.
- 제목은 가능하면 한 줄로 작성하고, 변경 목적이 드러나게 씁니다.
- 허용하는 주요 type은 다음과 같습니다.

```text
feat: 새 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 포맷팅 또는 스타일 수정
refactor: 동작 변경 없는 코드 구조 개선
test: 테스트 추가 또는 수정
chore: 빌드, 설정, 의존성, 기타 관리 작업
ci: CI 설정 수정
perf: 성능 개선
revert: 이전 커밋 되돌리기
```

예시:

```text
docs: 사용자 문서 구조 정리 및 버전 문서 추가
fix: 채널 캐시 만료 처리 오류 수정
feat: 플랫폼 자막 업로드 관리자 추가
chore: Docusaurus 버전 선택 설정 추가
```

## Git Workflow

- 사용자가 제외하라고 한 파일은 스테이징하거나 커밋하지 않습니다.
- 커밋 전에는 `git status --short`와 staged 파일 목록을 확인합니다.
- 이미 존재하던 사용자 변경은 임의로 되돌리지 않습니다.
- 관련 없는 변경이 워킹트리에 있으면 그대로 두고, 요청 범위에 해당하는 파일만 명시적으로 스테이징합니다.
- 커밋 메시지를 작성할 때 영어 기본 메시지로 대체하지 않습니다.

## Documentation

- `Docs_web/docs`는 Docusaurus current 문서이며, 현재는 Beta 문서로 취급합니다.
- `Docs_web/versioned_docs/version-1.0.0`은 v1.0.0 문서 스냅샷입니다.
- Beta에 들어가면 안 되는 v1 전용 문서는 `Docs_web/docs`에 추가하지 않습니다.
- v1 전용 문서 구조나 사이드바는 `versioned_docs`와 `versioned_sidebars` 쪽에서 관리합니다.

## Code Changes

- 기존 코드 스타일과 파일 구조를 우선합니다.
- 불필요한 리팩터링이나 포맷 변경을 섞지 않습니다.
- 문서나 UI 텍스트는 사용자가 명시하지 않는 한 한국어를 우선합니다.
- 민감 정보, API 키, 로컬 전용 설정은 커밋하지 않습니다.
